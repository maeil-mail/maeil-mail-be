spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        highlight_sql: true
  sql:
    init:
      mode: never

logging:
  level:
    org:
      hibernate:
        orm:
          jdbc:
            bind: TRACE
      springframework:
        transaction:
          interceptor: TRACE
