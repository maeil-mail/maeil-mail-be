spring:
  main:
    allow-bean-definition-overriding: true

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        auto_quote_keyword: true
        format_sql: true
    show-sql: true

  sql:
    init:
      mode: never

admin:
  secret: "secret"

distribute:
  server:
    count: 1
    index: 1
